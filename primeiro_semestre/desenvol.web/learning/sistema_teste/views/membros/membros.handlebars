<h3>Membros</h3>

<a href="/membros/novo" class="btn btn-secondary">Novo</a>

<table class="table" id="membros">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Cidade</th>
            <th>Telefone</th>
            <th>Idade</th>
            <th>Ocupação</th>
            <th>Vezes que assistiu Logan</th>
            <th>Contribuição</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each listamembros}}
        <tr> <!-- Table ROW --> 
            <td>{{this.id}}</td> <!-- Table DATA --> 
            <td>{{this.nome}}</td>
            <td>{{this.cidade}}</td>
            <td>{{this.telefone}}</td>
            <td>{{this.idade}}</td>
            <td>{{this.ocupacao}}</td>
            <td>{{this.vezesqueassistiu}}</td>
            <td>{{this.contribuicao}}</td>
            <td>
                <a href="/membros/alterar/{{this.id}}" class="btn btn-primary">Alterar</a>
                <button class="btn btn-danger js-delete" 
                        data-id="{{this.id}}">Excluir</button>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>


<div class="modal" tabindex="-1" role="dialog" id="myModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Excluir membro</h5>
      </div>
      <div class="modal-body">
        <p>Confirma a exclusão do membro?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" id="btnsim">Sim</button>
        <button type="button" class="btn btn-secondary" id="btncancelar" data-dismiss="modal">Cancelar</button>
      </div>
    </div>
  </div>
</div>

<script src="/js/jquery.min.js"></script>
<script src="/public/site.js"></script>

